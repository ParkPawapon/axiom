{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default least-privilege desktop capability for the AxiomPHP main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
