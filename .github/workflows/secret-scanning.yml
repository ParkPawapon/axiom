name: Secret Scanning
on:
  pull_request:
  workflow_dispatch:
jobs:
  placeholder:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - run: echo "Secret scanning policy placeholder"
