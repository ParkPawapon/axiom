# GitHub Branch Protection

Recommended production branch rules:

- Require pull requests before merging.
- Require code owner review.
- Require conversation resolution.
- Require status checks.
- Require branches to be up to date before merge.
- Block force pushes.
- Block deletions.
- Restrict bypass permissions to the repository owner only.

The intended bypass owner for this repository is `ParkPawapon`.

These settings should be enforced on `main` after the production foundation branch is merged.
